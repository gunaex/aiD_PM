services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./pm_system.db:/app/pm_system.db
      - ./backups_auto:/app/backups_auto
      - ./exports:/app/exports
    restart: unless-stopped
